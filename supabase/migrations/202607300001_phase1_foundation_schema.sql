-- Applied remotely as: phase1_foundation_schema
-- Reference copy for the repo. Prefer MCP / Dashboard migration history as source of truth.

-- Creates: enums, profiles (+ auth trigger), creator_profiles, admin_profiles,
-- categories (seeded), app_settings, RLS policies, is_admin().
